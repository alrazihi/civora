type: http
scheme: bearer
bearerFormat: JWT
description: 'JWT bearer token. The token must include `organization_id` claim.

  The API validates that the token''s organization matches the `{orgId}`

  path parameter on all tenant-scoped endpoints.

  '
